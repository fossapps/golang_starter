// +build integration

package migrations_test

import (
	//"crazy_nl_backend/migrations"
	"testing"
	//"crazy_nl_backend/helpers"
	//"crazy_nl_backend/config"
	//"github.com/stretchr/testify/assert"
)

func TestUserSeedPopulatesDb(t *testing.T) {
	//userSeed := migrations.UserSeed{}
	//session, err := helpers.GetMongo(config.GetMongoConfig())
	//defer session.Close()
	//assert.Nil(t, err)
	//assert.NotNil(t, session)
	//db := session.DB(config.GetTestingDbName())
	//defer db.DropDatabase()
	//migrations.Apply(userSeed, db)
}
